.grid_3
  = link_to dashboard_index_path :class => "logo" do
    = image_tag("headpin_logo.png", :alt => _('Katello Logo'))
    %span{:class => 'pipe'}
    %h1 Headpin
  #loading.fr
    =image_tag("spinner.gif", :alt => _('Spinning Work Indicator'))
.grid_13
  = hidden_field_tag 'root_url', nil, 'data-url' => root_path
  = hidden_field_tag 'get_notices_url', nil, 'data-url' => notices_get_new_path
  - if current_user
    - if working_org
      %div.org
        = render :partial => '/layouts/org'
    %ul
      %li.hello
        = link_to account_path do
          %strong
            = current_user.username
      %li.logout
        = link_to _("Logout"), logout_path
