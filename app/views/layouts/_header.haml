.grid_3
  = link_to dashboard_index_path :class => "logo" do
    = image_tag("logo.png", :alt => _('Katello Logo'))
    %span{:class => 'pipe'}
    %h1 Headpin
  #loading.fr
    %img{:src => '/images/spinner_head.gif', :alt => 'Spinning Work Indicator'}
.grid_13
  = hidden_field_tag 'root_url', nil, 'data-url' => root_path
  - if current_user
    - if working_org
      %div.org
        = render :partial => '/layouts/org'
    %ul
      %li.hello
        = link_to account_path do
          %strong
            = current_user.username
      %li.logout
        %a{ :href => "/logout"}
          #{_("Logout")}
