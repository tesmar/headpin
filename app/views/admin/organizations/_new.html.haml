= content_for :title do
  #{_("Create Organization")}

= content_for :content do
  .grid_8#new_organization123
    = form_tag admin_organizations_path, :method => "post", :id => "new_organization" do
      %fieldset
        .grid_2.ra
          %label #{_("Name")}:
        .grid_5.la
          = text_field_tag :displayName, params[:displayName], :tabindex => auto_tab_index
      .grid_5.push_2.la
        = button_to _("Save"), {:action => "create", :controller => "organizations"}, :method => :post, :id => :organization_save, :tabindex => auto_tab_index
